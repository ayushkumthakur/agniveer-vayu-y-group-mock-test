const admin=require('firebase-admin');const serviceAccount=require('./serviceAccountKey.json');const email=process.argv[2];if(!email)throw new Error('Usage: node set-admin.js admin@example.com');admin.initializeApp({credential:admin.credential.cert(serviceAccount)});(async()=>{const u=await admin.auth().getUserByEmail(email);await admin.auth().setCustomUserClaims(u.uid,{admin:true});console.log('admin=true set for',email)})().catch(e=>{console.error(e);process.exit(1)});
